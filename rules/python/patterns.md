# Python Patterns

- **FastAPI**: dependency injection, Pydantic models, async handlers
- **Django**: fat models thin views, signals for decoupling
- Testing: pytest + coverage, factory_boy for fixtures
- Async: `asyncio` with proper task management, no fire-and-forget
- Overlap check: before adding pip package, search `requirements.txt` and existing `lib/`