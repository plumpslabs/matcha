---
description: "🍵 Intent Discovery — plan before touching code"
---
# /matcha why

Run Intent Discovery before coding.

Say `/matcha why` to trigger `@matcha-planner` — answers What/Why/How, finds reuse evidence, writes plan to `.agents/plan/current.md` before the first edit.
