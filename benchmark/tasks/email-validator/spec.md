# Email Validator

Create a JavaScript file called `solution.js` that exports a function.

## Specification
Write a function `isValidEmail(email)` that returns `true` for valid emails and `false` for invalid ones.

## Requirements
- File must be named `solution.js`
- Export using `module.exports = { isValidEmail };`
- No external dependencies
