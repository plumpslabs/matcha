---
description: "🍵 Disable matcha — completely silent, zero token overhead"
---
# /matcha off

Disable all matcha lifecycle hooks for this session.

Run `matcha off` in terminal, or just say `/matcha off` — all guards are immediately silenced with zero token overhead until re-enabled.
