---
description: Session health dashboard — mode, hooks, active plan
---

# matcha status

Show the matcha session health dashboard.

Run `matcha status` in the terminal to see: current mode (on/off), intensity (observe/enforce/audit), hook states, and active plan summary.
