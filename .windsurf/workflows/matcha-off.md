---
description: Disable matcha — completely silent, zero token overhead
---

# matcha off

Disable all matcha lifecycle hooks.

Run `matcha off` in the terminal. All guards are immediately silenced with zero token overhead until re-enabled.
