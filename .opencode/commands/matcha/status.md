---
description: "🍵 Session health dashboard — mode, hooks, active plan"
---
# /matcha status

Show the matcha session health dashboard.

Say `/matcha status` or run `matcha status` in terminal to see: current mode (on/off), intensity (observe/enforce/audit), hook states, and active plan summary.
