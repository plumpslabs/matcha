# Testing Standards

- Tests must be deterministic
- Test behavior, not implementation
- Minimum 80% coverage on new code
- Name: `TestXxx` or `test_xxx` per language convention
- Matcha: before commit → test suite must pass