# TypeScript Patterns

- **React**: hooks over HOCs, composition over inheritance
- **Zustand** for state management (existing in project)
- API calls: typed fetch wrapper, error boundaries
- Testing: Vitest + Testing Library, MSW for API mocking
- Overlap check: before adding npm package, search `package.json` and existing utils